route:
  group_by: ['alertname', 'namespace']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'kubeshift-webhook'

receivers:
  - name: 'kubeshift-webhook'
    webhook_configs:
      - url: 'http://kubeshift-core:8080/api/v1/alerts'

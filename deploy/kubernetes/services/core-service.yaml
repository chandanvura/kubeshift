apiVersion: v1
kind: Service
metadata:
  name: kubeshift-core
  namespace: kubeshift
  labels:
    app: kubeshift-core
spec:
  type: ClusterIP
  selector:
    app: kubeshift-core
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
